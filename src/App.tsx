/**
 * LOGICAL FP — Root application entry
 * Keep the project as a single static landing page unless new routes are required.
 */

import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
